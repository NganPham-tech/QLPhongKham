@{
    Layout = null;
}
<!DOCTYPE html>
<html>
<head>
    <title>Test No Layout</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
    <div class="container mt-4">
        <h1>🧪 Test No Layout - Tạo Hóa Đơn</h1>
        <div class="alert alert-success">
            <strong>✅ View rendering hoạt động!</strong>
        </div>
        
        <div class="alert alert-info">
            <strong>Completed Appointments:</strong> 
            @if (ViewBag.CompletedAppointments == null)
            {
                <span class="badge bg-danger">NULL</span>
            }
            else
            {
                var appointments = (List<QLPhongKham.Models.Appointment>)ViewBag.CompletedAppointments;
                <span class="badge bg-success">@appointments.Count appointments</span>
            }
        </div>
        
        <!-- Test Tabs -->
        <ul class="nav nav-tabs mb-3" id="testTabs" role="tablist">
            <li class="nav-item" role="presentation">
                <button class="nav-link active" id="tab1-tab" data-bs-toggle="tab" 
                        data-bs-target="#tab1" type="button" role="tab">
                    Tab 1 - Từ cuộc hẹn
                </button>
            </li>
            <li class="nav-item" role="presentation">
                <button class="nav-link" id="tab2-tab" data-bs-toggle="tab" 
                        data-bs-target="#tab2" type="button" role="tab">
                    Tab 2 - Tạo mới
                </button>
            </li>
        </ul>
        
        <div class="tab-content" id="testTabContent">
            <div class="tab-pane fade show active" id="tab1" role="tabpanel">
                <div class="alert alert-warning">
                    <h5>Tab 1 Content</h5>
                    <p>Nếu thấy nội dung này thì tabs hoạt động bình thường.</p>
                    
                    @if (ViewBag.CompletedAppointments != null)
                    {
                        var appointments = (List<QLPhongKham.Models.Appointment>)ViewBag.CompletedAppointments;
                        if (appointments.Any())
                        {
                            <div class="table-responsive">
                                <table class="table table-striped">
                                    <thead>
                                        <tr>
                                            <th>Ngày khám</th>
                                            <th>Bệnh nhân</th>
                                            <th>Trạng thái</th>
                                        </tr>
                                    </thead>
                                    <tbody>
                                        @foreach (var appointment in appointments)
                                        {
                                            <tr>
                                                <td>@appointment.AppointmentDate.ToString("dd/MM/yyyy HH:mm")</td>
                                                <td>@appointment.Patient?.FullName</td>
                                                <td><span class="badge bg-success">@appointment.Status</span></td>
                                            </tr>
                                        }
                                    </tbody>
                                </table>
                            </div>
                        }
                        else
                        {
                            <div class="alert alert-info">Không có cuộc hẹn nào.</div>
                        }
                    }
                </div>
            </div>
            <div class="tab-pane fade" id="tab2" role="tabpanel">
                <div class="alert alert-info">
                    <h5>Tab 2 Content</h5>
                    <p>Form tạo hóa đơn mới sẽ ở đây.</p>
                </div>
            </div>
        </div>
    </div>
    
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
    <script>
        console.log('✅ Bootstrap JS loaded');
        console.log('ViewBag.CompletedAppointments:', @Html.Raw(Json.Serialize(ViewBag.CompletedAppointments ?? new List<object>())));
    </script>
</body>
</html>
